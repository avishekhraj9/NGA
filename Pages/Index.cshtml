@page
@model ReportingSystem.Pages.IndexModel

<h2>Report Generator</h2>

<form method="post">
    <button type="submit">Generate Report</button>
</form>

@if (Model.Result != null)
{
    <p>@Model.Result</p>
}